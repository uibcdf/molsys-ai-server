# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.whl

# PyInstaller
#  Usually these files are written by a python script acting as a frontend to an autotool-based build system
#  or other build system for C/C++ projects.
#  This applies to PyFuzzer, PyInstaller, or even a simple `setup.py` installed in `develop` mode.
#  For example: `python setup.py install --single-version-externally-managed --record=record.txt`
record.txt

# Unit test / coverage reports
.coverage
.pytest_cache/
htmlcov/
.nox/
.hypothesis/

# Type checker (Mypy, Pyright, etc)
.mypy_cache/
.dmypy.json
.pyre/
.pytype/
# Mypy reports generated by `mypy --txt-report`
mypy-report/

# Editor/IDE files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*~
.DS_Store
Thumbs.db

# Conda environment
.conda/
.env/
venv/
env/

# RAG generated index
data/rag_index.pkl

# Sphinx build outputs
docs/_build/

# Model server configuration (can contain sensitive info, typically managed outside VCS)
server/model_server/config.yaml

# chat_api generated corpus + index (snapshotted from sibling repos)
server/chat_api/data/docs/
server/chat_api/data/rag_index.pkl
server/chat_api/data/anchors.json

# Temporary build script
_build_index_script.py

# Jupyter Notebook checkpoints
.ipynb_checkpoints/
/models/

# Codex CLI session artifacts
codex_session.sh
