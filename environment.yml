name: molsys-ai
channels:
  - uibcdf
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  # MolSysSuite tools (from the laboratory channel)
  # - molsysmt
  # - molsysviewer
  # - topomt
  # - elastnet
  # - pharmacophoremt
  # Install Python deps via pip after creating the environment:
  #   pip install -e ".[dev]"
  #
  # vLLM is intentionally not installed here. See `dev/RUNBOOK_VLLM.md` for the
  # validated inference environment (pip + cu129 + system CUDA toolkit).
